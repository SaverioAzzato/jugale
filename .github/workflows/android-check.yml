name: Android Update Check

# Desktop cargo check cannot compile Kotlin or merge the Android manifest. Build a debug APK and
# run JVM tests when updater/share wiring changes, so failures are caught before a tag.
on:
  push:
    branches:
      - main
    paths:
      - "src-tauri/plugins/android-updater/**"
      - "src-tauri/plugins/android-share/**"
      - "src-tauri/capabilities/android.json"
      - "src-tauri/src/lib.rs"
      - "src-tauri/Cargo.toml"
      - "src-tauri/Cargo.lock"
      - "src/update/**"
      - "src/share/**"
      - ".github/workflows/android-check.yml"
  pull_request:
    paths:
      - "src-tauri/plugins/android-updater/**"
      - "src-tauri/plugins/android-share/**"
      - "src-tauri/capabilities/android.json"
      - "src-tauri/src/lib.rs"
      - "src-tauri/Cargo.toml"
      - "src-tauri/Cargo.lock"
      - "src/update/**"
      - "src/share/**"
      - ".github/workflows/android-check.yml"
  workflow_dispatch:

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest
    env:
      NDK_VERSION: "27.0.11902837"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - name: Install NDK
        run: sdkmanager "ndk;${{ env.NDK_VERSION }}"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Initialize Android project
        run: npx tauri android init --ci
        env:
          NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/${{ env.NDK_VERSION }}

      - name: Build debug APK
        # The app's debug-only application ID suffix changes `${applicationId}` but not the
        # generated MainActivity namespace. Disable it in this production-wiring check so the
        # merged share manifest matches the stable/Dev release builds this workflow protects.
        run: npx tauri android build --apk --debug --ci --config '{"bundle":{"android":{"debugApplicationIdSuffix":null}}}'
        env:
          NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/${{ env.NDK_VERSION }}

      - name: Test pure Kotlin plugin rules
        working-directory: src-tauri/gen/android
        run: ./gradlew testDebugUnitTest --no-daemon
